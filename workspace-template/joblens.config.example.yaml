version: 1
locale: ko-KR
ranking:
  scoreBands:
    veryStrong: 80
    strong: 70
    viable: 60
    stretch: 50
sources:
  manual:
    enabled: true
  saramin:
    enabled: false
    # Keep the real key in SARAMIN_ACCESS_KEY, never in this file.
  webSearch:
    enabled: true
privacy:
  publicRepoContainsPrivateData: false
